export interface FilmType {
  title: string
  episode_id: string
  release_date: string
  url: string
  characters: string[]
}
export interface PeopleType {
  name: string
  birth_year: string
  gender: string
  mass: string
}
